# DB Commands

\l — List all databases.
\c — Connect to a different database: \c another_database.
\d — List tables in the current database.
\dt — List all tables.
\d table_name — Show details for a specific table (columns, indexes, etc.).
\q — Quit psql.